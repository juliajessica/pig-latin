//back-end logic
var translate = function (sentence) {
 sentence=sentence.trim();
 var result = "";

var splitSent = sentence.split (" ");
  for (var z = 0; z < input.length; z += 1) {

  var splitWord = words[z].split("");


  var isCharacter = "";
    if (input.match(\b[a-z]*\b/i)) {



    return true;
  } else {
    return false;
  }
}; //End of 'isCharacter' function.

var isVowel = function(input) {
  if (input.charAt(0).match([aeiou]/i)) {
  isVowel = input + "way";
  // var one = input (0, 1)
  // var two = input.length (0, 1)
  }
};

var isNotVowel = function(input) {
  for(i = 0; i > input.results; i++) {
    if (true) {
      isNotVowel + wordSlice + "way"
    var = ()
    var =  ()
    }

var isQU = function(input) {
  var qu = input.charAt(0)+input.charAt(1);
  if (qu = "qu") {
    return true;
  } else {
    return false
  }
};


  }
}
